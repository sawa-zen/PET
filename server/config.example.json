{
  "claudeApiKey": "your-api-key",
  "systemPrompt": "You are a helpful assistant.",
  "mcpServers": {
    "sample-mcp": {
      "command": "npx",
      "args": ["sample-mcp"],
      "env": {
        "API_KEY": "your-api-key"
      }
    }
  }
}
